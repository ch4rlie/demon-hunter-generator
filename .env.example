# Cloudflare Worker URL for image transformation
# After deploying your worker, update this with your actual worker URL
# Example: https://transform-image.your-subdomain.workers.dev
VITE_WORKER_URL=http://localhost:8787

# Legacy Supabase variables (no longer needed if using Cloudflare Worker)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
